//! easytidy 无头 one-shot CLI（M1 起实现）。
//!
//! 子命令（计划）：list / create / destroy / snapshot / restart / rebuild /
//! run / boot / export / revoke。共享 easytidy-core 引擎。

fn main() {
    eprintln!("easytidy: not implemented yet (M1)");
    std::process::exit(1);
}
